<%= form_for (@task) do |t| %>
  <%= t.label :title, "enter a title" %>
  <%= t.text_field :title %>
  <%= t.label :details, "enter the details" %>
  <%= t.text_field :details %>
  <%= t.label :completed, "Completed" %> <%= t.check_box :completed %>
  <%= t.submit %>
<% end %>
